# T01 工单交付记录（独立工程）

- 日期：2026-09-08
- 状态：完成。双端可启动，依赖已锁定，未创建 git 提交（由项目负责人提交）。
- 范围声明：本工单仅建立可运行骨架；规格书 §6 数据契约、§7 掌握公式、§12 API 合同均未实现，自 T02 起按合同实施。

## 一、改动 / 新增文件清单

```text
backend/
  pyproject.toml            新增  FastAPI/Pydantic/SQLAlchemy/uvicorn 依赖声明 + pytest 配置
  .python-version           新增  锁定 3.12（本机解析为 CPython 3.12.13，uv 托管）
  uv.lock                   生成  uv 依赖锁文件（32 个包，含哈希）
  requirements.txt          生成  uv export 导出的 pip 兼容 pinned 清单（备选安装方式）
  app/main.py               新增  FastAPI 入口（create_app、CORS、/healthz）
  app/infrastructure/config.py 新增  pydantic-settings 读取仓库根 .env
  app/{api,domain,services,infrastructure}/__init__.py  新增  规格书 §14 分层目录
  app/__init__.py           新增
  tests/test_health.py      新增  /healthz 健康检查测试
  .venv/                    生成  本地虚拟环境（已 gitignore）
frontend/
  （由 create-vite 9.2.0 react-ts 模板并入：index.html、vite.config.ts、
   tsconfig*.json、.oxlintrc.json、src/App.tsx、src/main.tsx 等）
  package.json              修改  包名 xueyoujing-frontend、版本 0.1.0
  package-lock.json         生成  npm 依赖锁文件（27 个包，0 漏洞）
  index.html                修改  lang=zh-CN、标题「学有所径」（§13 短名）
  README.md                 重写  前端运行命令
  src/{api,components,pages}/  已存在（.gitkeep），T03/T08 启用
data/                        新增  运行数据目录（整目录 gitignore，规格书 §14）
README.md                    修改  状态、快速启动命令、进度标记
docs/T01-工单交付记录.md      新增  本文件
```

`.env.example`、`.gitignore`、`curriculum/quadratic/.gitkeep` 在仓库初始化时已存在，本工单未改动，仅验证了 `Settings` 可按其字段加载。

## 二、实际运行命令与结果

工具链：Windows 11，无系统 Python；使用 uv 0.11.21（托管 CPython 3.12.13）、Node v24.18.0、npm 11.16.0。

```text
cd backend
uv sync
  → Resolved 32 packages；安装 30 个直接/间接包，创建 .venv
uv run pytest -v
  → tests/test_health.py::test_healthz_returns_ok PASSED（1 passed, 2 warnings in 0.90s）
uv run uvicorn app.main:app --host 127.0.0.1 --port 8000
curl http://127.0.0.1:8000/healthz
  → {"status":"ok"}

cd frontend
npm install
  → added 27 packages, 0 vulnerabilities；生成 package-lock.json
npm run build
  → tsc -b 通过；vite build ✓ 20 modules，built in 216ms
npm run dev
curl -o NUL -w "%{http_code}" http://localhost:5173/
  → HTTP 200，页面标题「学有所径」
```

验证结束后两个后台服务均已停止，未遗留监听进程。

## 三、锁定的依赖版本（实测解析结果）

后端（Python 3.12.13）：fastapi 0.141.1、pydantic 2.13.5、pydantic-settings 2.15.0、
SQLAlchemy 2.0.52、uvicorn 0.52.4、starlette 1.6.0、pytest 9.1.1、httpx 0.28.1。
前端（Node 24）：vite 8.2.2、react 19.2.8、react-dom 19.2.8、typescript 6.0.3、
@vitejs/plugin-react 6.1.1、oxlint 1.82.0、@types/react 19.2.18。

## 四、T01 验收证据对照

| 验收项 | 结果 |
|---|---|
| 后端可启动 | 通过（uvicorn + /healthz 200） |
| 前端可启动 | 通过（vite dev 首页 200，build 通过） |
| .env 配置样例 | 通过（沿用已有 .env.example，Settings 按 .env 加载成功） |
| 依赖锁定 | 通过（backend/uv.lock + .python-version + requirements.txt；frontend/package-lock.json） |
| 无旧项目运行时依赖 | 通过（全仓 grep `ai[-_]agent|study[-_]assistant`，仅 README 与规格书文档性提及，代码 0 引用、0 import） |

## 五、观察项与合同说明（无静默变更）

1. `/healthz` 为规格书 §12 合同之外的基础设施端点，仅用于启动探活，不属于业务 API（业务接口统一前缀 `/api/v1` 自 T02 起实现）。如评审认为多余可删除，探活可改用 `/api/v1` 首个端点。
2. 依赖告警：starlette 1.6.0 提示 `TestClient` 基于 httpx 的用法已弃用（建议迁移 httpx2）。本工单按当前可用组合锁定并测试通过；T02 搭建 Repository 事务测试时再决定是否迁移。
3. 合同冲突记录：未发现。T01 未触碰 §12 业务接口与 §7 掌握公式；`.env.example` 字段为规格书授权的拟定方案（T02/T07 最终确定）。

## 六、修订记录

- 2026-09-08 v2：应用户要求前端开发端口由 5173 改为 **5174**（vite.config.ts `server.port=5174` + `strictPort: true`，被占用时报错而非漂移）；后端 CORS 默认白名单与 `.env.example` 的 `FRONTEND_DEV_URL` 同步改为 http://localhost:5174。同时把 create-vite 模板默认首页替换为项目占位页（学有所径 + 后端连接状态），删除未使用的模板资产（hero.png、react.svg、vite.svg、icons.svg）。本文第二、三节的 5173 探活结果为当时实测记录，端口调整后已在 5174 重新探活通过。

## 七、下一工单（T02 数据与事务）需要的信息

- 运行方式：`cd backend && uv sync && uv run ...`；测试 `uv run pytest`。
- 数据库约定：`DATABASE_URL=sqlite:///./data/app.db`（data/ 已创建且整体 gitignore）；路径相对进程 CWD，T02 建引擎时需显式解析到仓库根。
- 配置入口：`backend/app/infrastructure/config.py`（Settings，env_file 指向仓库根 `.env`，extra=ignore）。
- CORS 已按 `FRONTEND_DEV_URL`（默认 http://localhost:5173）放行，跨端口联调无需额外配置。
