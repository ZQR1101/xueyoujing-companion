# 题库人工抽检记录（进入 T05 前）

- 日期：2026-09-08
- 范围：curriculum/quadratic/questions.json 全部 24 题（6 知识点 × 4 题族）
- 抽检执行：执行模型自查（按项目负责人指定的五个重点）；**建议项目负责人对答案再复核一遍**，当前"人工"核验者仍为执行模型。

## 1. 题目与知识点映射

24/24 映射正确。逐题核对：

| 知识点 | 题目 | 考核点 | 结论 |
|---|---|---|---|
| C01 平方与代入 | F1-F4 | 负数平方 / 分数平方 / 负数代入求值 / (-2)² 与 -2² 辨析 | ✓ |
| C02 函数输入输出 | F1-F4 | 一次函数代入 / x² 代入 / 负数代入 x²+1 / 复合式小数代入 | ✓ |
| C03 二次函数图像 | F1-F4 | 图像识别 / 顶点识别 / 平移后顶点纵坐标 / 开口方向 | ✓ |
| C04 顶点式 | F1-F4 | 顶点读取 / 加号陷阱横坐标 / 系数不影响顶点 / 顶点处取值 | ✓ |
| C05 开口与对称轴 | F1-F4 | 负系数开口 / 对称轴读取 / 加号陷阱对称轴 / (6-x)² 变形 | ✓ |
| C06 简单应用 | F1-F4 | 落体代入 / 面积平方 / 平移边长面积 / 逆解方程 | ✓ |

无跨概念错位。

## 2. 答案与解析正确性

24/24 答案复核无误（含重算）：(-3)²=9、(1/2)²=1/4、(-5)²=25、2×3-1=5、(-2)²+1=5、(2.5-1)²+2=4.25、(x-1)²+2 顶点 (1,2)、(x+3)²-1 顶点横坐标 -3、2(x-4)²+5 顶点 (4,5)、(6-x)²=(x-6)² 对称轴 x=6、20-5×1²=15、7²=49、(3+2)²=25、5t²=20→t=2。
自动自检（评分器对标准答案判定 correct）在每次导入时运行，24/24 通过。

## 3. 难度合理性

初筛题（F1）难度 1～2，符合"入口筛查"定位；迁移/复习题难度 2～3。两处备注（非阻塞）：
- Q-C01-F4（(-2)² vs -2² 辨析）标为难度 1，实际是经典易错点，建议下版调为 2；
- Q-C06-F4 需要逆向解方程并取正根，难度 3 合理，但依赖"开平方取正"的约定，讲解资料 C06.md 已覆盖。

## 4. 题族区分度（是否同一道题改数字）

5 个知识点的 4 题族分别覆盖不同考核侧面（识别 / 计算 / 变形 / 辨析），**1 处同质化备注**：
- **Q-C01-F1 与 Q-C01-F3 部分同质**：都是"负数平方"（F1 选择题、F3 数值题）。格式与用途不同（screening vs transfer），暂不影响演示；建议课程包下版把 F3 改为"代入含平方的复合式"（如 x=3 时求 2x²+1）以拉开区分度。
- Q-C02-F2 与 F3 亦相近（x² 类代入），但 F3 增加负数与常数项，可接受。

结论：题族机制成立，无"同一题改数字刷分"问题；1 项改进建议记录在案。

## 5. 提示是否泄露完整答案

**发现问题并已修复**：原 24 题的第 4 级提示（L4"提供下一步局部过程"）普遍把最终答案甚至选项字母直接写出（如"所以 (-3)²=9，选 B"），违反 §10"局部过程"定位与 A15 精神。

修复（2026-09-08）：24 条 L4 提示全部重写为"给步骤、不给结论"，例：
- Q-C01-F1 L4：原"…所以 (-3)²=9，选 B" → 现"先算 3×3，再确定符号；把得数代回选项核对。"
- Q-C06-F4 L4：原"t² = 4，取正值得 t = 2" → 现"由 5t²=20 先求出 t²，开平方时记得时间取正值。"

修复后双重把关：
- 一次性泄露自检脚本通过（L4 不得含"选 X"、不得含最终答案完整表述）；
- 新增永久测试 `test_hints_do_not_reveal_answers`（选择题查"选 X"；小数/分数答案查值泄露；整数答案因单字符子串误报率高，由本记录人工复核覆盖）。

L1（提醒概念）/ L2（关键关系）/ L3（不同数字类比）复核：均未直接给出本题答案，符合 §10 分级定义。

## 结论

题库可支撑 T04 演示与 T05 开发。遗留改进（不阻塞）：
1. Q-C01-F3 与 F1 拉开区分度（下版课程包）；
2. Q-C01-F4 难度调 2；
3. 答案建议由项目负责人做最终人工复核。

---

# 第二批扩题抽检记录（2026-09-09）

- 范围：curriculum/quadratic/questions.json 新增 24 题（每知识点新增题族 F5-F8），全库 48 题、每概念 8 题族
- 抽检执行：执行模型自查；**建议项目负责人对答案再复核一遍**。

## 1. 题目与知识点映射

24/24 映射正确，每概念新增四族仍覆盖四个用途（screening/practice/transfer/review）：

| 知识点 | 新增题目 | 考核点 |
|---|---|---|
| C01 平方与代入 | F5-F8 | 负数平方 / 分数平方 / -6² 无括号辨析（transfer）/ 四式判断（review） |
| C02 函数输入输出 | F5-F8 | 一次式代入 / 二项式代入 / 带括号平移式代入 / 负分数代入 2x²-x（review, d3） |
| C03 二次函数图像 | F5-F8 | 顶点 (0,k) / 对称性（±5 同值）/ 上下平移解析式 / 最值性质（review） |
| C04 顶点式 | F5-F8 | 顶点读取 / 加号陷阱横坐标 / 负系数顶点（d3）/ 顶点处取值 |
| C05 开口与对称轴 | F5-F8 | 正系数开口 / 加号陷阱对称轴 / |a| 与开口宽窄（d3）/ (9-x)² 变形 |
| C06 简单应用 | F5-F8 | 落体代入 / 小数边长面积（decimal 配置）/ 逆解 (x+2)²=49（d3）/ 落地时刻（d3） |

难度分布（全库 48 题）：d1×16、d2×24、d3×8；新批 mcq×13、numeric×11，与首批同构。

## 2. 答案正确性

24/24 新题答案重算复核无误；导入时评分器答案自检 48/48 通过（validate_course 内置）。

## 3. 提示泄露检查

- 新题提示按首批修复后的标准撰写：L4 只给步骤不给结论、不含"选 X"、不含最终答案表述；
- 自动化检查（与 `test_hints_do_not_reveal_answers` 同规则）48/48 通过；
- 整数答案人工复核备注（非泄露）：
  - Q-C03-F6（±5 对称性）：答案 25 在题干中已给出（"当 x = 5 时 y = 25"），题目本身考对称性识别，提示仅复述题干事实；
  - Q-C05-F8（(9-x)²）：题干与验证性提示（x=0 与 x=18 处 y=81）不可避免含数字 9，答案与题干常数重合；
  - Q-C02-F8：答案 1 为单字符，提示中以 1/2 分数碎片出现，已确认 L4（"两个 1/2 相加，合起来是多少？"）不给出结论。

## 4. 对测试与脚本的影响

抽题稳定排序（screening 优先、按 id 升序）使 C01 的抽取序列变为 F1 → F5 → F2 → F3 → F4 → F6 → F7 → F8。已同步：
- `tests/test_t03_curriculum.py`：题量断言 24 → 48；
- `tests/test_t04_flow.py` / `test_t05_planner.py` / `test_t06_tutoring.py` / `test_t07_agent.py`：答案映射扩展至新题；
- `scripts/smoke_t07.py`：练习题错答/重试改为选择题键位。

掌握度公式按证据条数计算（(对+1)/(总+2)），与题目身份无关，估值断言全部未动。全量测试 72/72 通过。

---

# 第三批扩题抽检记录（2026-09-09）

- 范围：curriculum/quadratic/questions.json 新增 252 题（每知识点新增 14 个题族 F9-F22，每族 3 个变体），全库 300 题、132 题族、每概念 50 题
- 生成方式：`scripts/generate_bank.py` 参数化模板批量生成（幂等，可重复执行补量）

## 1. 生成与核验机制（与人工批次的差异）

第三批题目由代码生成，正确性保障链与人工批次不同，说明如下：

- **答案由代码按题面定义精确计算**（整数 / Fraction / 精确小数，禁止浮点近似），不存在人工誊写错误；
- 生成后自动运行：提示泄露自检（mcq 不得含"选 X"、L4 不得含正确选项文本；小数/分数答案不得出现在任何提示；L4 不得含最终答案；解析不得与提示重复）→ 评分器答案自检（300/300）→ 课程包完整校验（validate_course）→ 全部通过才写回；
- **"人工"核验者仍为执行模型**：模板的教学质量（题意清晰度、干扰项合理性、提示分级）建议项目负责人抽样复核，重点抽查 mcq 干扰项与 L3 类比。

## 2. 结构与分布

- 每概念 14 个新题族：screening×3、practice×4、transfer×4、review×3；
- 全库用途分布：screening 66 / practice 84 / transfer 84 / review 66；mcq 119 / numeric 181；难度 d1×76 / d2×168 / d3×56；
- 新题 id 为 Q-<cid>-X01..X42，恒排在 F1-F8 之后（字符串序），保证既有抽题序列与测试语义不变。

## 3. 提示泄露检查

- 自动化检查 300/300 通过（规则同上）；
- L2 允许包含题面中间量（沿用 Q-C04-F2 先例，如"x + 6 = x - (-6)，所以 h = -6"），L4 一律不含数字与结论；
- 整数答案的 L2 中间量泄露沿用首批抽检口径：L4 与选项文本零泄露，L2 属"关键关系"分级允许范围。

## 4. 对测试与脚本的影响

- C01 抽取序列前几轮变为 F1 → F5 → X01（screening 跳队）→ F2 → ...：`test_t04_flow` / `test_t05_planner` 的三轮答案映射补充 Q-C01-X01（正确键位 A）；
- `test_a14` 题池耗尽改为动态：从数据库读 C01 全部题目标准答案逐轮答对，不写死轮数；
- `test_t03_curriculum` 题量断言 48 → 300；
- 全量测试 72/72 通过，导入评分器自检 300/300 通过。

## 结论

题库规模达到 300 题，可支撑演示与后续开发。遗留事项（不阻塞）：
1. 建议项目负责人对第三批模板抽样复核（每概念至少抽 2 个题族）；
2. 用途分布 screening/review（66）略低于 practice/transfer（84），属每概念 3.5 题族除不尽的正常结果，如需严格均衡可在下批微调。
