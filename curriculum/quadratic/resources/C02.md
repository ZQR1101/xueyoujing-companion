# 函数输入输出

## 概念

函数是一台“输入—输出”机器：给自变量 x 一个值，按规则算出唯一的 y。

例：y = 2x - 1，输入 x = 3，输出 y = 2×3 - 1 = 5。

## 代入的顺序

1. 把 x 的值写进表达式（建议加括号）；
2. 先算乘方；
3. 再算乘除；
4. 最后算加减。

例：y = (x-1)² + 2，x = 2.5：
x - 1 = 1.5，1.5² = 2.25，y = 4.25。

## 为什么重要

后面所有的图像描点、顶点求值、应用题，本质都是“给 x 算 y”。代入稳，函数才稳。
